---
export interface Link {
  href: string;
  text: string;
  isActive?: boolean;
}

type Props = Link;

const { href, text } = Astro.props;
---

<a
  href={href}
  class="text-lg font-medium text-neutral-500 transition-all hover:text-primary-500"
  >{text}</a
>
